# ============================================================
# 插件私有配置（可读写）
# 本文件由插件自身维护，与全局配置隔离
# ============================================================

# 插件级开关
enabled: true

# 示例：自定义参数（按需增减）
max_concurrent: 20
timeout_seconds: 60

# 通知相关
notify_recipients: ""   # 全局通知接收人（逗号分隔，可选）
